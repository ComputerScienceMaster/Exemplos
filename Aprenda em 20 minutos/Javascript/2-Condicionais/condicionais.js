var a = 7; //Atribuindo valor a variavel 'a'
var b = 11; //Atribuindo valor a variavel 'b'

if(a > b){ //Metodo de comparação
    //Caso 'a' seja maior do que 'b', isso será impresso
    console.log(a + " é maior do que " + b);
} else {
    //Caso não seja, isso será impresso
    console.log(a + " não é maior do que " + b);
}